#ifndef MENU_H
#define MENU_H

#include<stdio.h>
#include<stdlib.h>

#define TODO

void basic_func();
void array_func();
void string_func();
void AVLTree();


#define MIN(a,b) (((a)<(b))?(a):(b))
#define MAX(a,b) (((a)>(b))?(a):(b))
#define MAX_HEIGHT 50
#define GAP 1


#endif
